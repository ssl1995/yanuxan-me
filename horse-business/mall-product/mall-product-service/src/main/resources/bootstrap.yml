spring:
  application:
    name: @artifactId@

server:
  port: 4002
  servlet:
    context-path: /mall/product

dubbo:
  cloud:
    subscribed-services: 'mall-marketing-service,mall-base-service,horse-user-service'